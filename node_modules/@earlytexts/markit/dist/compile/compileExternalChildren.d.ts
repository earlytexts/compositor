import type { CompileOptions, MarkitDocument, MarkitError, Metadata } from "../types.js";
import type { TextTreeWithMetadata } from "./parseMetadata.js";
/**
 * Recursively compile external children from the children metadata field.
 * (Takes the same compile function as an argument to allow for recursion without circular imports.)
 */
declare const _default: <TextMetadata extends Metadata, BlockMetadata extends Metadata>(treeWithMetadata: TextTreeWithMetadata<TextMetadata, BlockMetadata>, options: CompileOptions, loadingStack: Set<string>, compile: (text: string, options: CompileOptions, loadingStack: Set<string>) => [MarkitDocument<TextMetadata, BlockMetadata>, MarkitError[]]) => [MarkitDocument<TextMetadata, BlockMetadata>[], MarkitError[]];
export default _default;
